import type { Evidence } from '../types/contracts'
export function EvidenceCard({ evidence }: { evidence?: Evidence }) { if(!evidence)return <div className="evidence-empty">Evidence appears only when external context is requested.</div>; return <section className="evidence"><div className="evidence__head"><h3>MARKET CONTEXT</h3><strong>{evidence.support_status==='partial'?'Partially supported':evidence.support_status}</strong></div><dl><dt>Source</dt><dd>{evidence.source_title}</dd><dt>Retrieved</dt><dd>{new Date(evidence.retrieved_at).toLocaleTimeString('en-GB',{hour:'2-digit',minute:'2-digit'})}</dd><dt>Freshness</dt><dd>{evidence.freshness}</dd><dt>Confidence</dt><dd>{Math.round(evidence.confidence*100)}%</dd></dl>{evidence.source_url&&<a href={evidence.source_url} target="_blank" rel="noreferrer">Open source</a>}</section> }

